import PremiumCard from "@/components/ui/PremiumCard";

export default function PopularPremiumCard() {
  return (
    <PremiumCard
      description="Unlock ad-free reading, early chapters, HD images and exclusive content."
      variant="gradient"
    />
  );
}
